import { DungeonGenerator } from "../DungeonGenerator";

export function smoothen(this: DungeonGenerator) {
  //TODO After this one, also check each door if its free
  // Same with chests and whatnopt
}
